defaults: &defaults
  aws_key: <%= ENV['AWS_KEY'] %>
  aws_secret: <%= ENV['AWS_SECRET'] %>
  tmdb_key: <%= ENV['TMDB_KEY'] %>
  s3_bucket: <%= ENV['S3_BUCKET'] %>

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
